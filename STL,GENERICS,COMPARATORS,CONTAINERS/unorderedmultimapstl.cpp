#include<bits/stdc++.h>
#define ll long long 
#define pb push_back
#define t int 
#define in cin
#define ff vector<int>
#define fastio ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define vp(it,arr)  for(auto it:arr){cout<<it<<;}
#define LOOP(i,a,b) for(int i=a;i<b;i++)
using namespace std;
int main(){
unordered_multimap<char,int>m1;
//These type of map will keep the similar keys values in randomised order and they are internally stored by using hashing
//and in these type of map the keys are not in sorted order

return 0;
}