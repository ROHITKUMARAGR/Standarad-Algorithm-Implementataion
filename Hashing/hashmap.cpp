#include<bits/stdc++.h>
#define ll long long 
#define pb push_back
#define all(n) n.begin(),n.end() 
#define vec vector<int>
#define fastio ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define vp(it,arr)  for(auto it:arr){cout<<it<<;}
#define LOOP(i,a,b) for(int i=a;i<b;i++)
using namespace std;
class Node{
  public:
  string arr="";
  int value=0;
  Node* next=NULL;
  Node(string arr,int value){
   this->arr=arr;
   this->value=value;
  }


};
class HashTable{
   public:
   int current_Size=0;
   int total_Size=1;
   
};
int main(){



return 0;
}